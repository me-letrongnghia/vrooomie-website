# ============================================
# VROOOMIE ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to git

# ============================================
# APPLICATION SETTINGS
# ============================================
# Domain/IP where the application is hosted
# Local: localhost
# VPS: your-domain.com or VPS_IP_ADDRESS
APP_DOMAIN=localhost
APP_PROTOCOL=http

# Ports (usually don't need to change)
BACKEND_PORT=8080
FRONTEND_PORT=80

# ============================================
# DATABASE SETTINGS
# ============================================
DB_HOST=mysql
DB_PORT=3306
DB_NAME=car_rental
DB_USERNAME=rental_user
DB_PASSWORD=change-this-strong-password

# MySQL Root Password
MYSQL_ROOT_PASSWORD=change-this-root-password

# ============================================
# REDIS SETTINGS
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
CACHE_TYPE=redis

# ============================================
# JWT SETTINGS
# ============================================
# Generate a strong secret key (min 256 bits)
# You can use: openssl rand -base64 64
JWT_SECRET_KEY=change-this-to-a-very-long-random-secret-key-minimum-256-bits

# ============================================
# OAUTH2 SETTINGS
# ============================================
# Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth2
FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret

# OAuth2 Configuration
OAUTH2_DEFAULT_USER_ROLE=RENTER

# OAuth2 Redirect URI
OAUTH2_REDIRECT_URI=your-redirect-uri-based-on-APP_DOMAIN-and-FRONTEND_PORT

# Frontend URL (for CORS)
FRONTEND_URL=your-frontend-url-based-on-APP_DOMAIN-and-FRONTEND_PORT

# ============================================
# EMAIL SETTINGS
# ============================================
# Gmail SMTP settings (you can use other providers)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-specific-password
MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS_ENABLE=true

# ============================================
# FRONTEND SETTINGS
# ============================================
# This will be constructed automatically based on APP_DOMAIN and APP_PROTOCOL
# Local: http://localhost:4200 (development) or http://localhost:80 (docker)
# VPS: http://your-domain.com or http://YOUR_VPS_IP
# No need to set manually - will be set by docker-compose

# ============================================
# CORS & REDIRECT SETTINGS
# ============================================
# These will be set automatically based on APP_DOMAIN and APP_PROTOCOL
# No need to change manually

# ============================================
# NOTES FOR DEPLOYMENT
# ============================================
# LOCAL DEVELOPMENT:
#   APP_DOMAIN=localhost
#   APP_PROTOCOL=http
#   FRONTEND_PORT=4200 (if running ng serve) or 80 (if using docker)
#
# VPS DEPLOYMENT:
#   APP_DOMAIN=your-vps-ip (e.g., 192.168.1.100) or your-domain.com
#   APP_PROTOCOL=http (or https if you have SSL)
#   FRONTEND_PORT=80 (or 443 for HTTPS)
#
# IMPORTANT: 
#   1. Copy this file: cp .env.example .env
#   2. Update .env with your actual values
#   3. Never commit .env to git
#   4. For OAuth2, update redirect URIs in Google/Facebook console to match your APP_DOMAIN
